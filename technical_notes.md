# 📚 Technical Notes

## 🏗️ プロジェクト構造

### 5分割バイナリアーキテクチャ
```
nekocode-workspace/
├── nekocode-core/     # 共通ライブラリ（型定義・セッション管理）
├── nekocode/         # Tree-sitter解析エンジン
├── nekorefactor/     # リファクタリングツール
├── nekoimpact/       # 影響度分析
├── nekoinc/          # インクリメンタル解析
└── nekomcp/          # MCP統合（未実装）
```

### 重要ファイルパス
- MCP設定: `~/.claude.json`
- MCPサーバー: `mcp-nekocode-server/mcp_server_real.py`
- セッションデータ: `.nekocode_sessions/`
- メモリデータ: `.nekocode_memories/`

## 🔧 実装詳細

### SymbolInfo構造の複雑性について
- **column_start/end**: リファクタリング精度に必須
- **symbol.id**: 依存関係追跡に必要
- **metadata**: 言語固有情報の保存
- 複雑だが必要な設計と判断

### Tree-sitter統合
- 7言語対応: JS/TS/Python/Rust/C++/Go/C#
- ASTクエリ使用で高速解析
- 各言語個別のパーサー設定必要

### セッション管理
- UUID v4でセッションID生成
- JSON形式で永続化
- インクリメンタル更新対応

## 🐛 既知の問題と回避策

### セマンティック位置指定の不正確さ
- 原因: 単純な文字列マッチング
- 回避: 行番号指定を使用
- 解決策: Smart版実装（Tree-sitter活用）

### move-linesコマンドの使いにくさ
- 行番号カウントが面倒
- ファイル名を2回指定必要
- 将来: シンボル指定に変更予定

### MCPサーバーのキャッシュ問題
- 変更が即反映されない
- Claude Code再起動が必要な場合あり

## 💡 設計決定の理由

### 即適用デフォルトの理由
- Git管理下では`git diff`/`git restore`で安全
- プレビュー→確認の2ステップは煩雑
- 9割のケースで即適用で問題なし

### smartサブコマンド分離の理由
- セッションモード混在は複雑
- デバッグが困難になる
- 段階的移行が可能

### Python選択の理由（TODOアプリ実験）
- ビルド不要で即実行
- インデント構造が明確
- 標準ライブラリで完結
- エラーメッセージが親切

## 🚀 パフォーマンス指標

### Tree-sitter vs PEGTL
- TypeScript 68ファイル: 1.2s vs 19.5s（16倍高速）
- 検出精度も向上

### インクリメンタル解析
- 初回: 267ms
- 更新: 23-49ms（918-1956倍高速）

## 📝 今後の改善案

### 優先度高
1. Smart版実装（Tree-sitter活用）
2. 自動インデント検出
3. 言語別ルール実装

### 優先度中
1. シンボルベースの位置指定
2. import自動整理
3. バッチ実行モード

### 優先度低
1. AI支援リファクタリング
2. クロスファイル操作
3. リモート解析